# 🚀 Как запустить и посмотреть приложение

## Быстрый старт

### 1️⃣ Откройте терминал (PowerShell) в папке проекта

```powershell
cd "C:\Users\Хентай монстр\Documents\заказ №1\it-hub-schedule"
```

### 2️⃣ Запустите dev-сервер

```powershell
# Если npm работает напрямую:
npm run dev

# Если PowerShell ругается на npm.ps1, используйте:
$nodePath="C:\Program Files\nodejs"
if(Test-Path "$nodePath\node.exe"){$env:Path+=";$nodePath"}
npm.cmd run dev
```

### 3️⃣ Откройте браузер

После запуска вы увидите что-то вроде:

```
  VITE v7.3.1  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

**Откройте в браузере:** http://localhost:5173/

---

## Что вы увидите:

1. **Сначала** — экран-заставка: "Здравствуйте, Кристина Александровна!"
2. **Через 2 секунды** — главная страница с тремя колонками:
   - **Слева** — меню с разделами (Преподаватели, Дисциплины, Аудитории и т.д.)
   - **По центру** — рабочая область (таблицы для редактирования)
   - **Справа** — панель "Заметки"

3. **Попробуйте:**
   - Кликните на "Список преподавателей" в левом меню
   - Добавьте/отредактируйте преподавателя в таблице
   - Создайте заметку справа (кнопка "+")
   - Данные сохраняются автоматически в LocalStorage браузера

---

## Остановка сервера

В терминале нажмите `Ctrl + C`

---

## Если что-то не работает:

### Проблема: "npm не найден"
**Решение:** Убедитесь, что Node.js установлен:
```powershell
$nodePath="C:\Program Files\nodejs"
if(Test-Path "$nodePath\node.exe"){$env:Path+=";$nodePath"}
node -v
npm.cmd -v
```

### Проблема: "Порт 5173 занят"
**Решение:** Vite автоматически выберет другой порт (5174, 5175 и т.д.). Смотрите в терминале, какой порт указан.

### Проблема: "Ошибки при сборке"
**Решение:** Удалите `node_modules` и переустановите:
```powershell
Remove-Item -Recurse -Force node_modules
npm.cmd install
npm.cmd run dev
```

---

## Сборка для продакшена

Если хотите собрать готовую версию:

```powershell
npm.cmd run build
```

Готовые файлы будут в папке `dist/`. Их можно залить на любой хостинг.

---

## Полезные команды

- `npm run dev` — запуск dev-сервера (горячая перезагрузка)
- `npm run build` — сборка для продакшена
- `npm run preview` — предпросмотр собранной версии
- `npm run lint` — проверка кода линтером

