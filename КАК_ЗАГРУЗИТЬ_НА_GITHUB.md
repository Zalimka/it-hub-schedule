# 🚀 Как загрузить проект на GitHub (пошаговая инструкция)

## Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com и войдите (или зарегистрируйтесь)
2. Нажмите зеленую кнопку **"New"** (или "+" → "New repository")
3. Заполните форму:
   - **Repository name**: `it-hub-schedule` (или любое другое название)
   - **Description**: `Генератор расписания для IT Hub колледжа`
   - Выберите **Public** (публичный) или **Private** (приватный)
   - ❌ **НЕ ставьте галочки** на "Add a README file", "Add .gitignore", "Choose a license" (у нас уже все есть!)
4. Нажмите **"Create repository"**

## Шаг 2: Скопируйте URL вашего репозитория

После создания репозитория GitHub покажет страницу с инструкциями. 
**Скопируйте URL** — он будет выглядеть так:
```
https://github.com/ваш-username/it-hub-schedule.git
```

## Шаг 3: Выполните команды в терминале

Откройте PowerShell в папке `it-hub-schedule` и выполните команды **по порядку**:

### Команда 1: Перейти в папку проекта
```powershell
cd "C:\Users\Хентай монстр\Documents\заказ №1\it-hub-schedule"
```

### Команда 2: Инициализировать Git (если еще не сделано)
```powershell
git init
```
**Что это делает:** Создает скрытую папку `.git` для отслеживания изменений

### Команда 3: Добавить все файлы
```powershell
git add .
```
**Что это делает:** Готовит все файлы к загрузке (точка = все файлы)

### Команда 4: Создать первый коммит (сохранение)
```powershell
git commit -m "feat: IT Hub Schedule Generator v1.0.0"
```
**Что это делает:** Сохраняет текущее состояние проекта с комментарием

### Команда 5: Переименовать ветку в main
```powershell
git branch -M main
```
**Что это делает:** Называет основную ветку "main" (современный стандарт)

### Команда 6: Подключить ваш GitHub репозиторий
```powershell
git remote add origin https://github.com/ВАШ-USERNAME/it-hub-schedule.git
```
**⚠️ ВАЖНО:** Замените `ВАШ-USERNAME` на ваш реальный username с GitHub!

**Что это делает:** Говорит Git, куда отправлять файлы

### Команда 7: Загрузить проект на GitHub
```powershell
git push -u origin main
```
**Что это делает:** Отправляет все файлы на GitHub

## ⚠️ Если возникли проблемы:

### Проблема: "Please tell me who you are"
Выполните эти команды (замените на свои данные):
```powershell
git config --global user.name "Ваше Имя"
git config --global user.email "ваш-email@example.com"
```

### Проблема: Запрос логина/пароля
GitHub больше не принимает пароли. Нужно использовать **Personal Access Token**:

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите срок действия и права (нужен `repo`)
4. Скопируйте токен (показывается только один раз!)
5. При запросе пароля вставьте токен

### Проблема: "remote origin already exists"
Выполните:
```powershell
git remote remove origin
```
Потом снова команду 6 с правильным URL

## ✅ Проверка

После выполнения всех команд:
1. Обновите страницу вашего репозитория на GitHub
2. Вы должны увидеть все файлы проекта!

## 📝 Что дальше?

После загрузки вы можете:
- Обновлять проект: `git add .` → `git commit -m "описание"` → `git push`
- Просматривать историю: `git log`
- Откатывать изменения: `git checkout -- файл`

---

**Нужна помощь?** Создайте issue в репозитории или обратитесь к документации Git.

