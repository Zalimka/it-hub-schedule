# 🖥️ Как открыть проект на другом компьютере

## Шаг 1: Установите Node.js (если еще не установлен)

1. Откройте https://nodejs.org/
2. Скачайте **LTS версию** (рекомендуется)
3. Установите, следуя инструкциям
4. Проверьте установку:
   ```powershell
   node --version
   npm --version
   ```

## Шаг 2: Установите Git (если еще не установлен)

1. Откройте https://git-scm.com/download/win
2. Скачайте и установите Git
3. Проверьте установку:
   ```powershell
   git --version
   ```

## Шаг 3: Клонируйте проект с GitHub

Откройте PowerShell или командную строку и выполните:

```powershell
# Перейдите в папку, куда хотите скачать проект (например, Рабочий стол или Документы)
cd C:\Users\ВашеИмя\Documents

# Клонируйте проект
git clone https://github.com/Zalimka/it-hub-schedule.git

# Перейдите в папку проекта
cd it-hub-schedule
```

## Шаг 4: Установите зависимости

```powershell
npm install
```

Это загрузит все необходимые библиотеки (может занять 1-2 минуты).

## Шаг 5: Запустите проект

```powershell
npm run dev
```

После запуска вы увидите что-то вроде:
```
  VITE v7.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

## Шаг 6: Откройте в браузере

Откройте браузер и перейдите по адресу: **http://localhost:5173**

---

## ✅ Готово!

Теперь проект работает на новом компьютере!

---

## 📝 Полезные команды:

- `npm run dev` - запустить проект для разработки
- `npm run build` - собрать проект для production
- `npm run preview` - посмотреть собранный проект

---

## 🔄 Если нужно обновить проект (после изменений на GitHub):

```powershell
# Перейдите в папку проекта
cd C:\Users\ВашеИмя\Documents\it-hub-schedule

# Загрузите последние изменения
git pull
```

---

## ⚠️ Если возникли проблемы:

### Проблема: "npm не найден"
**Решение:** Убедитесь, что Node.js установлен. Перезапустите PowerShell после установки.

### Проблема: "git не найден"
**Решение:** Установите Git и перезапустите PowerShell.

### Проблема: Ошибки при `npm install`
**Решение:** 
1. Удалите папку `node_modules` (если есть)
2. Удалите файл `package-lock.json` (если есть)
3. Выполните `npm install` снова

### Проблема: Порт 5173 занят
**Решение:** Закройте другие приложения, использующие этот порт, или используйте другой порт:
```powershell
npm run dev -- --port 3000
```

---

## 🚀 Быстрый запуск (если уже все установлено):

```powershell
cd C:\Users\ВашеИмя\Documents
git clone https://github.com/Zalimka/it-hub-schedule.git
cd it-hub-schedule
npm install
npm run dev
```

Откройте http://localhost:5173 в браузере!

